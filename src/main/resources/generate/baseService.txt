package {servicePackageName};

import {beanPackageName}.PageBean;
import {beanPackageName}.PageResult;
import {beanPackageName}.Result;

public interface BaseService<T, ID> {

    PageResult<T> findAll(T t);

    PageResult<T> findAll(PageBean pageBean, T t);

    T findById(ID id);

    long count(T t);

    Result update(T t);
    
    Result save(T t);

    Result deleteById(ID id);

    Result delete(T t);

    Result delete();
}
