package {daoPackageName};

import java.util.List;

import org.apache.ibatis.annotations.Param;

public interface BaseMapper<T, ID> {

    List<T> findAll();

    List<T> findAll(@Param("params") T t);

    T findById(ID id);

    long count(@Param("params") T t);

    int update(T t);
    
    int save(T t);

    int deleteById(ID id);

    int delete(T t);

    int delete();
}
